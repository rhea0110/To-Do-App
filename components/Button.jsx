/* import { useState } from 'react';
const Button = () => {
    const [text, setText] = useState('Button1')
    return(
        <div>
            <button type="submit" onClick={() => setText('Buttton changed')}>{ text }</button>
            </div>
    )
}

export default Button; */